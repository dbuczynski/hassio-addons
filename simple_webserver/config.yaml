name: "Simple Web Server"
version: "1.0.1"
slug: "simple_webserver"
description: "A simple, generic and configurable web server."
arch:
  - "aarch64"
  - "amd64"
startup: "services"
boot: "auto"
ports:
  80/tcp: 8081
map:
  - share:rw
options:
  document_root: "/share/simple_webserver"
schema:
  document_root: "str"
